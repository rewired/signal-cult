[package]
license-file = "../../../../LICENSE"
name = "broken-fm-companion"
version = "0.1.0"
description = "BROKEN FM OFX companion editor"
authors = ["rewired-vfx"]
edition = "2021"

[lib]
name = "broken_fm_companion_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
